# VigaBSS 5.0 — Pre-Launch Test Checklist

Manual / integration tests that only surface against a **real environment or external service** — the mocked-DB jest suite (CI) does not cover these. Mapped from the codebase by a multi-agent audit (2026-07-25).

**Status:** ✅ tested · 🟡 sandbox-only (needs production/real config) · ⚪ unit-only (code path tested, never run for real) · ⬜ untested.  
**Priority:** 🔴 P0 = launch blocker · 🟠 P1 = important · 🟢 P2 = nice-to-have.

> The four you named map as: **SW** + **Finkok** = 🟡 sandbox-tested (stamp AND cancel proven with TEST CSDs — the old Finkok cancel-format defect was resolved by PR #494; still need a *production* PAC contract + a *real* CSD). **WhatsApp** + **Stripe checkout** = ⚪ built & unit-tested, never run against a real provider yet.

## Payments & billing

- [ ] **Stripe hosted Checkout — full round trip (session → pay → webhook → reconcile → invoice paid)**  —  🔴 P0 · ⚪ unit-only
  - **Run:** On a real Stripe test-mode account attached to a payment_gateways row (secret_key_encrypted + webhook_secret_encrypted set), POST /portal/invoices/:id/pay, complete the payment on the real Stripe-hosted page, and let Stripe fire checkout.session.completed to POST /api/v1/payment-webhooks/stripe/:gatewayId. Confirm the pending payment_transactions row (matched by gateway_reference_id = the cs_... session id, or the metadata.transaction_id fallback) flips to succeeded, reconcilePayment marks the invoice 'paid', and the client_balance_ledger credit is written.
  - **Why:** This is the entire online-payment front door and it has never executed against real Stripe — only mocked unit tests. Every link is a silent-failure point: if the session-id UPDATE, the checkout.session.completed→payment_status:'paid' mapping, the webhook secret, or the tenant-scoped transaction match is even slightly off, the customer's card is charged at Stripe but the invoice stays unpaid and the balance never moves, with no error surfaced to anyone.
  - **Needs:** Real Stripe test-mode account + API secret key and webhook signing secret stored on the gateway row (encrypted, ENCRYPTION_KEY set); a publicly reachable webhook URL (Stripe CLI `stripe listen --forward-to` or a tunnel)
- [ ] **Stripe webhook signature verifies over the raw body THROUGH the nginx reverse proxy**  —  🔴 P0 · ⚪ unit-only
  - **Run:** Send a genuinely Stripe-signed event (Stripe CLI or a real dashboard test event) at the production nginx→Express stack and confirm verifyStripeSignature passes. The HMAC is computed over `${t}.${rawBody}`, and rawBody is only captured by the express.json verify hook for /payment-webhooks paths — verify nginx (docker-compose.prod / host-nginx) forwards the byte-exact body, doesn't re-chunk/re-encode/buffer-limit it, and that the Stripe-Signature/Digest headers survive.
  - **Why:** verifyStripeSignature/verifyConektaSignature are byte-exact HMACs. Anything in the real transport that mutates the body (proxy re-serialization, gzip, a body size limit below Stripe's payload, header case-folding) makes every real webhook 401 while unit tests (which hand the function a pristine string) stay green. Result: no webhook ever reconciles in production and the failure is invisible until someone notices invoices never auto-mark paid.
  - **Needs:** Deployed nginx + app stack (docker-compose.prod or host-nginx) and a real Stripe-signed payload (Stripe CLI/dashboard); confirm STRIPE_WEBHOOK_SECRET or per-gateway secret matches
- [ ] **Recurring autopay charges a real saved card off-session (portal enrollment → mandate → unattended charge)**  —  🔴 P0 · ⚪ unit-only
  - **Run:** As a subscriber, click "Enable autopay" on /portal/account → complete the Stripe Checkout SETUP-mode page with a test card → verify the setup webhook stores a recurring_payment_profiles row (pm_ + cus_ + card brand/last4, is_default). Then run process_recurring_charges against real Stripe and verify money actually moves (charged=true, not a perpetual 'pending'), the linked invoice settles via payment_transactions.invoice_id, and DELETE /portal/autopay revokes.
  - **Why:** The whole chain is built (Checkout setup-mode capture collects the SCA mandate; chargeStripe sends customer + off_session=true + payment_method) but has NEVER run against real Stripe — mocks return 'succeeded'. The real-only risks: the setup-mode mandate not actually authorizing the later off-session PaymentIntent (authentication_required decline), the setup webhook's metadata-driven gateway resolution on the env-var route, and the one-active-default unique guard under Stripe's webhook redelivery.
  - **Needs:** Real Stripe test-mode account attached to a payment_gateways row (secret + webhook secret)
- [ ] **Conekta hosted checkout end-to-end (the dominant MX gateway: card + OXXO + SPEI)**  —  🟠 P1 · ⚪ unit-only
  - **Run:** Configure a real Conekta gateway and have a subscriber pay an invoice online: the Pay button must redirect to Conekta's hosted page (POST /orders with an embedded HostedPayment checkout); pay with a test card, then let Conekta fire order.paid (Digest header) to POST /payment-webhooks/conekta/:gatewayId and confirm the tx (matched by the ord_… reference) reconciles and settles the linked invoice. Also try the OXXO cash path (voucher renders; 72h expiry).
  - **Why:** The hosted-checkout flow is built to the Conekta v2 Orders/Checkout spec but has NEVER run against real Conekta — no sandbox account was available. Real-only risks: Conekta's customer_info validation (name/email/phone completeness), the HostedPayment checkout.url actually returning, and the order.paid id matching the stored create-response id. For an MX-focused launch where Conekta is the primary local processor, this is the first thing to prove.
  - **Needs:** Real Conekta account (secret + webhook key) on a payment_gateways row
- [ ] **Duplicate webhook delivery does not double-credit (real provider redelivery / replay)**  —  🟠 P1 · ⚪ unit-only
  - **Run:** Deliver the same succeeded event twice (Stripe auto-retries on any non-2xx, or replay the event id via Stripe CLI / Conekta resend) and confirm webhook_events dedup on (provider, provider_event_id, org_dedup) short-circuits the second one so the ledger is credited once and the invoice is marked paid once.
  - **Why:** Providers redeliver constantly (on timeouts, 5xx, or operator replay). reconcilePayment is best-effort and runs inside handleWebhookEvent; the only guard against double-crediting a client is the webhook_events dedup + the FOR UPDATE lock. That dedup path (org_dedup generated column, migration 417) only ever runs against mocked rows in CI — a real duplicate with a real event id is the only way to prove the partition key and the dedup query actually match and money isn't credited twice.
  - **Needs:** Real Stripe/Conekta account able to redeliver, or manual event replay against a real MySQL (generated org_dedup column must exist)
- [ ] **Refund of a gateway (Stripe/Conekta) payment reverses the invoice + balance**  —  🟠 P1 · ⚪ unit-only
  - **Run:** Take an invoice paid via real Stripe/Conekta online checkout, then refund it (via refund(transactionId) and via a real FULL charge.refunded webhook) and verify: the settled invoice reverts to 'issued', an offsetting 'debit' ledger row is written, the client's computed balance shows the amount owed again, and a PARTIAL refund does NOT reopen the invoice. Redeliver the same charge.refunded and confirm no double-reversal.
  - **Why:** reverseRefund keys off payment_transactions.settled_invoice_id (recorded at reconcile time) and is idempotent, with a full-refund gate on charge.refunded — all built and unit-tested, but the round-trip has never run against a real provider. Real-only risks: Stripe's actual charge.refunded payload shape (refunded/amount_refunded fields on partial refunds) and webhook redelivery timing.
  - **Needs:** Real Stripe/Conekta account (to capture then refund) + real MySQL
- [ ] **Invoice numbering + billing-period generation under real concurrent MySQL**  —  🟠 P1 · ⚪ unit-only
  - **Run:** On a real MySQL (test:db/prod, InnoDB), run the billing cycle concurrently for the same org (parallel generateInvoice / auto_generate_invoices) and confirm invoice numbers are gapless, unique, and never trip the uq_invoices_org_number unique key, and that a billing_period is never double-invoiced.
  - **Why:** nextInvoiceNumber depends on the INSERT IGNORE + `UPDATE ... LAST_INSERT_ID(next_number)+1` sequence idiom and an InnoDB row lock; generateInvoice depends on `SELECT ... FOR UPDATE` on the billing_period. The mocked jest DB cannot exercise row locking, LAST_INSERT_ID semantics, or the unique-key collision at all — so the entire race-safety story is unverified against a real engine. A real concurrency test is the only way to prove two simultaneous billing runs don't 500 on a duplicate invoice number or emit two invoices for one period.
  - **Needs:** Real MySQL/InnoDB (docker-compose.test or prod) with concurrent invocation; not the mocked CI DB
- [ ] **Credit note / refund-as-credit against a stamped invoice produces a stamped CFDI de Egreso**  —  🟠 P1 · 🟡 sandbox-only
  - **Run:** For an MX org, issue a credit note against an already-stamped (vigente) invoice, then stamp it via `POST /credit-notes/:id/stamp` (or the 🧾 Stamp CFDI button on /credit-notes). Verify the CFDI de Egreso (TipoDeComprobante='E', serie egreso, UsoCFDI G02, PUE) is stamped vigente at the PAC, carries `CfdiRelacionados TipoRelacion="01"` with the original invoice's UUID, and downloads/cancels like any CFDI from /cfdi.
  - **Why:** In Mexico, crediting/refunding a stamped ingreso legally requires a stamped egreso CFDI related to the original; without it SAT records overstate income. **Sandbox-proven 2026-07-25** (an MX-locale org: egreso ABB56BF5… stamped vigente via Finkok sandbox, CfdiRelacionados→ingreso UUID accepted, TFD present; 409/422 guards fired live). Still needs the production repeat with a real CSD.
  - **Needs:** Real PAC contract (SW/Finkok) + real CSD
- [ ] **One-currency-per-org holds across the gateway charge amount, CFDI, and statements (no 'USD' default leak)**  —  🟢 P2 · ⚪ unit-only
  - **Run:** Provision a fresh MXN org and walk a full cycle: generate invoice, record/allocate a payment, run a real Stripe charge, stamp the CFDI, and render the statement PDF — asserting every money surface is MXN and Stripe receives currency:'mxn' with the correct cents.
  - **Why:** payments.currency (and several sibling tables) carry `DEFAULT 'USD'` at the schema level, and past bugs had 'USD' leaking into ledgers/charges. chargeStripe/createStripeCheckoutSession pass currency straight through to Stripe's amount-in-cents — a wrong or defaulted currency means the customer is charged in the wrong denomination (and Stripe may reject MXN-vs-USD zero-decimal assumptions). Config/data drift like this only shows on a clean real org exercised end-to-end through a real processor; the mocked tests never assert the outbound currency Stripe actually sees.
  - **Needs:** Clean org set to MXN + real Stripe test account to observe the charged currency/amount; real MySQL to expose column defaults
- [ ] **Scheduled late-fee / dunning job fires in a real deployment and skips stamped invoices**  —  🟢 P2 · ⚪ unit-only
  - **Run:** In a real deployment with the scheduler/taskRunner running, let apply_late_fees and send_payment_reminders execute on a genuinely overdue invoice (real NOW()/DATEDIFF/timezone) and confirm: the fee lands as a taxable line keeping total = subtotal + tax_amount, invoice_late_fees dedup respects max_applications, and any invoice carrying a vigente/cancel_pending CFDI is left untouched.
  - **Why:** These are switch-cased scheduled tasks (taskRunner) — a seeded task with no running scheduler simply never fires, silently, so late fees are never collected. The date math (DATEDIFF(NOW(), due_date), CURDATE()) is timezone-sensitive and only correct under the server's real clock/TZ, not a mocked one. And the 'skip invoices with a live CFDI' guard is critical: applying a fee to a stamped invoice desyncs it from the document already filed at SAT. Only a real deploy with the scheduler on, a real overdue date, and a real stamped invoice exercises all three.
  - **Needs:** Deployed app with cron/taskRunner enabled + real MySQL + correct server timezone; an overdue invoice and a stamped-CFDI invoice to prove the skip

## CFDI / MX fiscal

- [ ] **SW production stamp with a real CSD + real SW contract (both seal modes)**  —  🔴 P0 · 🟡 sandbox-only
  - **Run:** On a production-environment org, add the real SW production PAC row (real infinite token or user/pass) and upload the org's REAL CSD (.cer/.key + passphrase), then stamp a real income CFDI (tipo I) end-to-end through BOTH seal modes: Emisión (seal_mode=pac, SW seals our unsealed XML via /cfdi33/issue/json/v4) and Timbrado (seal_mode=local, our engine seals, SW stamps via /cfdi33/stamp/v4). Confirm a vigente UUID, a real SAT sello, and a downloadable signed XML.
  - **Why:** Everything to date is SW SANDBOX (services.test.sw.com.mx) with SAT TEST CSDs. Production uses a different host, a real contracted stamping tier (Timbrado vs Emisión — SW pricing/tier still pending), and a real CSD; the is_test_certificate guard hard-blocks a test cert against a prod PAC, so the first real stamp is the first time the entire prod path runs. If the contracted tier does not match the row's seal_mode, every invoice silently fails to stamp and the first real customer invoice is unbillable. Also: swAuthToken reads pac.token_encrypted / pac.password_encrypted VERBATIM and the pacProviders route stores those columns via generic CRUD without calling encrypt() — a real prod token pasted in the UI is stored plaintext and used as-is; must verify auth actually succeeds.
  - **Needs:** Real SW production contract + token (or user/pass), the org's real CSD (.cer/.key/passphrase), org pac_environment=production
- [ ] **Finkok production stamp with a real CSD + real Web Services creds**  —  🔴 P0 · 🟡 sandbox-only
  - **Run:** Configure a production Finkok PAC row (facturacion.finkok.com) with real WS API credentials (NOT the portal login) and the org's real CSD, then stamp an income CFDI. Because Finkok ALWAYS local-seals, this simultaneously exercises our seal engine against Finkok's production SOAP endpoint.
  - **Why:** Finkok is proven only against demo-facturacion.finkok.com with reseller creds (demo_platform/hex) and TEST CSDs. In the demo, the Finkok PORTAL login was rejected by the WS API (needed WS creds / API-enable) — the same blocker will recur in prod. Production host + real WS creds + real CSD have never been exercised. If Finkok is the primary PAC, no invoice stamps at all.
  - **Needs:** Real Finkok Web Services creds with API enabled, the org's real CSD, production env
- [ ] **MX default IVA 16% + per-client Exento resolved into a stamped CFDI**  —  🔴 P0 · 🟡 sandbox-only
  - **Run:** (a) Fresh MX org, real client, no explicit tax rate → generate invoice; confirm a 16% IVA line and that the CFDI concepto carries ObjetoImp='02' + TipoFactor='Tasa' TasaOCuota=0.160000, and a real PAC accepts it. (b) Set the client tax_exempt → confirm the CFDI emits ObjetoImp='02' + TipoFactor='Exento' with NO TasaOCuota/Importe and NO TotalImpuestosTrasladados, and the PAC accepts it. (c) Confirm a plain non-exempt 0-rate does NOT masquerade as Exento.
  - **Why:** The code side is SHIPPED and hardened — this row is now purely about PAC acceptance. resolveTaxContext applies the org default with a 16% IVA safety net for MX orgs (#512/#514/#516); migration 427 makes that default deterministic by guaranteeing at most one active default per org (#548); and both create paths now REJECT a document that carries no tax when a rate applies, rather than silently emitting 0% (#549). What remains unproven is only that a real PAC accepts each of the three treatments — Tasa 16%, Exento, and a genuine non-exempt 0-rate — since SAT and client accounting treat them differently and all three still stamp successfully in sandbox. No user decision is outstanding.
  - **Needs:** Real PAC + CSD to confirm PAC acceptance of each treatment. (Nothing else — this line used to also ask for "user confirmation of the MX default-IVA/exemption rules", contradicting the Why directly above it, which states no user decision is outstanding. The rules were confirmed and shipped in #512/#514/#516.)
- [ ] **CSD upload → encryption-at-rest → decrypt-at-stamp round-trip in real prod**  —  🔴 P0 · 🟡 sandbox-only
  - **Run:** With ENCRYPTION_KEY set in the production env, upload the org's real CSD; inspect the DB and confirm key_pem_encrypted/passphrase_encrypted are CIPHERTEXT (not plaintext), then stamp and confirm the key decrypts and signs. Then (a) boot prod with ENCRYPTION_KEY UNSET → upload must 422 ENCRYPTION_REQUIRED; (b) rotate/lose ENCRYPTION_KEY and confirm the failure mode when an existing CSD can no longer be decrypted.
  - **Why:** encryption.encrypt() is a transparent no-op without ENCRYPTION_KEY, so a prod install missing the key would store the CSD PRIVATE KEY in plaintext — the NODE_ENV=production hard-stop is the only thing preventing that and it only triggers in production. A wrong/rotated key makes every stamp fail at decrypt with no data-loss warning. Only surfaces with a real key + real CSD in a real container.
  - **Needs:** Production deploy with ENCRYPTION_KEY (64-char hex), the org's real CSD, and DB access to inspect stored ciphertext
- [ ] **Finkok cancellation end-to-end (production)**  —  🟠 P1 · 🟡 sandbox-only
  - **Run:** Stamp via Finkok production, then cancel through Finkok for motivo 02 (no relation) and motivo 01 (with folioSustitucion). Confirm SAT returns EstatusUUID 201/202 and a real acuse is stored and the invoice syncs to cancelled.
  - **Why:** The old signature-format defect ('wrong signature length' / 'Invalid Signature') was RESOLVED by PR #494 (Finkok's cancel wants base64 of PEM cer + the DECRYPTED key as unencrypted PKCS#8 PEM, and a self-closing `apps:UUID` element) and is **sandbox-proven twice** — 2026-07-22 (#494's own walk) and re-proven 2026-07-25 (UUID 9EA26552… cancelled via Finkok, EstatusUUID 201, acuse persisted, doc → cancelado). Production host + a real CSD have never exercised it, and motivo 01 (with folioSustitucion) hasn't been walked even in sandbox.
  - **Needs:** Real Finkok WS creds (production, API-enabled) + the org's real CSD
- [ ] **SW production cancellation (inline-CSD) with a real acuse + bookkeeping sync**  —  🟠 P1 · 🟡 sandbox-only
  - **Run:** Stamp via SW prod, then cancel via /cfdi33/cancel/csd using the org's real CSD material (b64 DER cer/key + passphrase). Verify EstatusUUID 201/202, the acuse is stored, the invoice auto-transitions to cancelled, allocations/money are released, and a REP'd invoice cancels its complement FIRST.
  - **Why:** SW cancel is sandbox-verified (201) only. Production signs the real Solicitud de Cancelación with the real CSD; a passphrase or DER-encoding mismatch surfaces only against SAT prod. Critically, syncInvoiceCancelled → cancelInvoiceForSat is best-effort/non-fatal: a sync failure after SAT accepts leaves a SAT-cancelled invoice still sitting in receivables and every financial report, diverging the books from SAT silently.
  - **Needs:** Real SW production contract + the org's real CSD
- [ ] **REP payment complement (Pago 2.0) production stamp incl. multi-currency parcialidad**  —  🟠 P1 · 🟡 sandbox-only
  - **Run:** Partially pay a stamped PPD invoice, let auto-REP fire, and stamp the tipo-P CFDI in production. Cover the same-currency MXN path (TipoCambioP=1, EquivalenciaDR=1) AND a cross-currency case (MonedaP≠MonedaDR → real EquivalenciaDR factor) across a multi-installment chain (ImpSaldoAnt/ImpPagado/ImpSaldoInsoluto rollup).
  - **Why:** REP is legally mandatory for PPD invoices. Only the simple MXN path is sandbox-proven; the CRP20215/CRP20238 currency rules and the parcialidad saldo math are the classic SAT rejections. A wrong EquivalenciaDR or saldo silently yields rejected/uncancelable complements, putting the org out of compliance on every installment payment received.
  - **Needs:** Real PAC + real CSD, a PPD invoice, and ideally a non-MXN client for the cross-currency leg
- [ ] **PAC failover across two REAL providers — double-stamp safety**  —  🟠 P1 · ⚪ unit-only
  - **Run:** Configure BOTH SW and Finkok as production PACs with priorities. Force the primary UNREACHABLE (firewall-block its host) mid-stamp and confirm failover to the secondary produces exactly ONE UUID. Then force the primary to TIMEOUT / return a PAC error and confirm it does NOT fail over (no second stamp).
  - **Why:** The failover contract (fail over only on isUnreachable, never on timeout/response-error; retry the same provider with identical sealed XML) is unit-tested only. The catastrophic real risk — the same document stamped at SAT by BOTH providers, a duplicate CFDI that's painful to unwind — can only manifest with two live accounts and a genuine network partition, which no jest test can reproduce.
  - **Needs:** Two real production PAC accounts (SW + Finkok) on one org, plus the ability to induce a network outage of the primary
- [ ] **CSD expiry monitor actually fires alerts (scheduler + notifications + email)**  —  🟠 P1 · ⚪ unit-only
  - **Run:** With the scheduler running in prod, seed CSDs at valid_to within 60/30/7 days and one already past expiry. Confirm csd_expiry_monitor flips the lapsed cert to status='expired'+is_active=0, writes the notification bell once (dedup by title), emails admin/manager recipients, and logs the 'NO recipients' warning for an org with no active admin/manager instead of silently dropping it.
  - **Why:** A CSD expires every ~4 years and an expired CSD halts ALL stamping (activeOrgCsdRow throws CSD_EXPIRED). The alert chain depends on the scheduler being enabled, taskRunner dispatching csd_expiry_monitor, real recipients existing, and SMTP configured — none of which jest exercises. If any link is missing, the first symptom of an expired CSD is a total billing outage with no prior warning.
  - **Needs:** Scheduler/taskRunner enabled in prod, SMTP configured, seeded near-expiry CSD rows
- [ ] **pac_environment isolation — sandbox rows can never stamp a production invoice**  —  🟠 P1 · ⚪ unit-only
  - **Run:** On an org with BOTH a sandbox and a production PAC row configured, set organization_mx_profiles.pac_environment='production' and confirm ONLY the production row stamps (sandbox creds never touch SAT); switch to 'sandbox' and confirm the reverse. Confirm a 'simulator' row with environment='production' is refused.
  - **Why:** This single switch decides which credentials/endpoint reach SAT. A mis-scoped selection would let a sandbox PAC stamp a real customer invoice (fiscally void) or a production PAC burn real stamps on test data. The scoping logic is unit-tested, but its real consequence — which SAT endpoint receives the live document — only shows with real dual-environment credentials on one org.
  - **Needs:** Both a sandbox and a production PAC account configured on the same org
- [ ] **Representación impresa PDF renders and its QR verifies at SAT**  —  🟢 P2 · 🟡 sandbox-only
  - **Run:** After a real production stamp, generate the representación impresa PDF. Confirm it renders in the prod container (pdfkit fonts + qrcode), carries the TFD (UUID, SAT/CSD cert numbers, SelloSAT, total con letra), and that scanning the QR opens verificacfdi.facturaelectronica.sat.gob.mx and shows the CFDI as vigente with the matching total.
  - **Why:** The printed PDF is the customer-facing artifact; the TFD-required guard means it only works on a genuinely stamped CFDI. A wrong tt (total) or fe (last-8 of sello) in the QR, or a font/render failure in the container, means every invoice PDF is broken or fails SAT verification — invisible until a customer actually scans it.
  - **Needs:** A real stamped CFDI (real PAC + CSD) and the production container (fonts available)

## RADIUS & network

- [ ] **PPPoE subscriber auth against a real NAS (Access-Request -> Access-Accept + rate policy)**  —  🔴 P0 · ⬜ untested
  - **Run:** Create a PPPoE contract (subscriberProvisioningService generates creds -> radius row), run the RADIUS sync, then dial a real PPPoE client through a real MikroTik/CHR pointed at VigaBSS's RADIUS. Confirm Access-Accept, that PAP AND CHAP passwords both authenticate, and that the Mikrotik-Rate-Limit VSA actually shapes the session to the plan speed. Run it BOTH ways: embedded RADIUS server (RADIUS_SERVER_ENABLED=true, radiusServerService.buildAcceptAttributes/generateAttributes) and external FreeRADIUS 3.x reading the synced radcheck/radgroupreply tables (syncFreeradiusTables).
  - **Why:** Every jest test mocks the DB and never emits a UDP packet, so the on-wire VSA encoding (radiusCoaEncoder.encodeVSA length math), PAP/CHAP decode, and plan->attribute mapping have never been parsed by a real NAS. Worse, there are TWO independent attribute writers (embedded generateAttributes vs FreeRADIUS radgroupreply) plus the FreeRADIUS README's own custom authorize queries against the radius table — if they disagree, a subscriber authenticates but is uncapped, gets the wrong speed, or is silently rejected, with nothing in CI catching it.
  - **Needs:** Real MikroTik/CHR NAS with a PPPoE server + a dial-in client; VigaBSS RADIUS reachable (embedded 1812/1813 or external FreeRADIUS + rlm_sql). Lab CHR (74.208.108.182 / fireisp-nas-3) exists but the dial-in auth path is unverified.
- [ ] **RADIUS accounting ingest (Start/Interim/Stop) from a real NAS into connection_logs**  —  🔴 P0 · ⬜ untested
  - **Run:** Bring a real subscriber online through the NAS and let it send real accounting to VigaBSS: via the embedded acct server (UDP 1813, radiusServerService.handleAcct) and via the FreeRADIUS rest-module POST (/radius/accounting). Verify connection_logs rows are written, octets+Gigawords combine correctly (combineOctetsGigawords), the Accounting-Request authenticator validates, the session shows online (getActiveSession/getSessionByClientId), and a Stop closes it and rolls up usage.
  - **Why:** Usage totals, data caps, 'who's online', top-talkers, and suspension liveness all derive from connection_logs. If the authenticator check rejects real packets, or Gigawords wrapping is wrong, usage is silently zero or off by 4GB multiples; if the accounting-response is malformed the NAS retransmits forever. None of this is exercised without a real NAS emitting real accounting.
  - **Needs:** Real NAS emitting RADIUS accounting; RADIUS_ACCOUNTING_SECRET set (for the FreeRADIUS-rest path) or embedded acct server enabled.
- [ ] **Suspend -> Disconnect-Request actually drops the live session on the NAS**  —  🔴 P0 · ⬜ untested
  - **Run:** With a subscriber online, trigger a suspension (or kickDuplicateSessions) and confirm the RADIUS Disconnect-Request (code 40, to CoA port 3799) actually tears down the live PPPoE session on the real NAS and returns a Disconnect-ACK.
  - **Why:** suspensionService.sendRadiusPacket builds the Disconnect with ONLY a User-Name attribute — no Acct-Session-Id, NAS-IP-Address, or Framed-IP — so a NAS that matches sessions on session-id/IP may reply NAK or ignore it, leaving non-payers online. And a UDP timeout is returned as {sent:true}, so VigaBSS records the disconnect as successful even when the NAS never answered. Only a real DAE listener reveals this.
  - **Needs:** Real MikroTik with RADIUS incoming (CoA/DAE) accept=yes on 3799 and a live session to kill.
- [ ] **Full lifecycle: overdue -> walled-garden -> pay -> reactivation without manual touch**  —  🔴 P0 · ⬜ untested
  - **Run:** Let an overdue invoice drive walledGardenSuspendContract (CoA sets Mikrotik-Address-List + FreeRADIUS re-sync), confirm the subscriber is redirected to the captive portal by the seeded firewall/NAT rules, record a payment, and confirm walledGardenReconnect (CoA remove + re-sync) restores full service. Verify the address-list actually clears on the live session, not just at next re-auth.
  - **Why:** This is the money path. Reconnection depends on the NAS honoring a CoA that removes an address-list from an already-established session; many NAS only apply address-list membership at session start, so a paying customer can stay walled until they reconnect. The seedDevice forward-reject + dst-nat rules must also actually exist and be ordered correctly for the portal to be reachable while everything else is blocked.
  - **Needs:** Real NAS seeded with the walled-garden rules (seedDevice seedWalledGarden), a reachable captive portal, and the billing suspension scheduler running.
- [ ] **WhatsApp Wi-Fi password reset actually reaches a real TR-069 ONU**  —  🔴 P0 · ⚪ unit-only
  - **Run:** From a real WhatsApp number, run the self-service 'reset wifi' flow: confirm the new PSK is emailed, then confirm the queued SetParameterValues task (PreSharedKey.1.PreSharedKey) is genuinely applied on a real TR-069 ONU and the client's Wi-Fi actually changes. Time how long between the WhatsApp confirmation and the CPE actually applying it.
  - **Why:** THE silent break. There is no TR-069 connection-request/wake mechanism anywhere in the code — the task sits in cpe_tasks until the ONU's next periodic Inform (minutes to hours; possibly never for a NAT'd GPON ONU that the ACS can't reach). Yet resetWifiPassword marks the portal_service_request 'completed' and returns applied:true the instant the task is queued. The customer is told 'done' while their Wi-Fi is unchanged, then gets kicked off mid-use whenever the CPE finally Informs. Plus the CWMP Basic-auth + SetParameterValues XML (cwmpXml/cwmpSessionService) has never been parsed by a real vendor's CPE stack.
  - **Needs:** Real TR-069 ONU (e.g. Huawei/ZTE GPON) configured to Inform VigaBSS's /acs/cwmp endpoint with acs_username/acs_password; a real Twilio or Meta WhatsApp number + verified webhook.
- [ ] **CoA live speed change (plan change / time-based speed window)**  —  🟠 P1 · ⬜ untested
  - **Run:** Change a live subscriber's plan (or cross a plan_access speed-window boundary) and confirm a CoA carrying the new Mikrotik-Rate-Limit is accepted (CoA-ACK) and the subscriber's real throughput changes without reconnecting. Let a speed window activate while syncFreeradiusTables also runs.
  - **Why:** speedWindowService and syncFreeradiusTables must compute byte-identical rate strings for the same plan; if they drift, the two writers fight forever (transition + CoA fan-out every tick). And the CoA itself must be accepted and honored by the real NAS mid-session — unit tests can't observe either the throughput change or the fan-out storm.
  - **Needs:** Real NAS with an active shaped session + a throughput test (e.g. iperf/speedtest) before/after.
- [ ] **IP pool assignment matches the address the subscriber actually receives**  —  🟠 P1 · ⬜ untested
  - **Run:** Assign an address from an ip_pool (poolAssignmentService) to a PPPoE subscriber, bring them online, and confirm the IP the NAS actually hands out (seen in accounting Framed-IP-Address) matches VigaBSS's ip_assignments record.
  - **Why:** Neither syncFreeradiusTables nor the embedded RADIUS server emits a pool-derived Framed-IP-Address or Framed-Pool (Framed-Pool=88 is defined in the codec but never used) — only a static radius.ip_address is emitted. So dynamic pool addresses are really assigned by the NAS's own local-address-pool, and VigaBSS's ip_assignments/usage can silently diverge from what's on the wire. Any IP-keyed lookup (CGNAT, abuse/DMCA, billing-by-IP) then fingers the wrong customer.
  - **Needs:** Real NAS doing PPPoE dynamic addressing from a pool that VigaBSS believes it owns.
- [ ] **One-click MikroTik seedDevice bootstrap against real RouterOS (idempotent re-run)**  —  🟠 P1 · 🟡 sandbox-only
  - **Run:** Run routerProvisioningService.seedDevice against a real MikroTik/CHR over the binary API and verify each step lands: RADIUS client (service=ppp), PPP AAA use-radius+accounting+interim, RADIUS incoming CoA listener, queue-tree/priority queues, PPPoE server+profile, walled-garden reject+redirect, and realtime/VoIP DSCP. Then re-run and confirm every step reports unchanged/updated (non-destructive) and nothing is duplicated or re-disabled.
  - **Why:** This is the prerequisite that makes tests 1-6 even possible, and it is dozens of RouterOS API command strings with version-specific quirks (priority dual-field '2/2', api-ssl port 8729, API 'true'/'false' vs CLI yes/no, place-before ordering, max-limit=0 == unlimited) validated only by reading against ROS 7.21. One wrong word aborts a step or silently mis-shapes the router; a bad idempotency check re-disables a rule the admin re-enabled.
  - **Needs:** Real RouterOS with binary-API credentials (api_username + encrypted api_password) and a real ENCRYPTION_KEY to decrypt them. Lab CHR exists but the binary-API seed path is unconfirmed (memory only notes REST-over-http).
- [ ] **CPE zero-touch provisioning: real ONU Informs -> ACS -> profile tasks applied**  —  🟠 P1 · ⬜ untested
  - **Run:** Factory-reset ONU boots and Informs VigaBSS's /acs/cwmp (HTTP Basic). Verify handleInform upserts the device, ZTP matches a cpe_profile by manufacturer/OUI, buildProvisioningTasks queues tasks, and the real ONU accepts the CWMP SetParameterValues XML and applies the config across the session state machine.
  - **Why:** cwmpXml build/parse and the cwmpSessionService state machine have never met a real vendor's CWMP/SOAP stack — namespace, cwmp:ID header, and fault handling differ per vendor and a mismatch silently stalls provisioning. The ACS endpoint is mounted outside the authenticated /api/v1 surface and must be reachable from the CPE network with correct Basic-auth; a routing/auth gap means devices Inform but never provision.
  - **Needs:** Real TR-069 ONU on a network that can reach the ACS HTTP(S) endpoint, with a matching cpe_profile seeded.
- [ ] **SNMP polling of real devices (v2c + v3) with correct scaling and no overflow-nulling**  —  🟠 P1 · 🟡 sandbox-only
  - **Run:** Point snmpPoller at real devices of each managed type (MikroTik router, UBNT AirMAX radio, GPON OLT, UPS/SFP) using both v2c and encrypted v3 credentials. Verify if_in/out_octets, cpu_usage, memory_usage (hrStorage RAM ratio), and RF metrics (signal/CCQ/noise/SNR) populate with correct transforms and that no real value gets nulled by METRIC_COLUMN_BOUNDS.
  - **Why:** Dashboards, fleet glance, top-talkers, and alerts all read snmp_metrics; a wrong per-vendor OID or transform silently yields nulls/garbage rather than an error, and the SNMPv3 authKey/privKey decrypt path only works with a real ENCRYPTION_KEY against a device that enforces authPriv. The hrStorage memory ratio and UBNT signal/CCQ OIDs are device-model-specific and can't be validated by mocks.
  - **Needs:** Real SNMP devices per model (esp. UBNT AirMAX + an OLT) and a real ENCRYPTION_KEY for the v3 key decrypt. Only RouterOS memory% (28.70) is live-verified so far.
- [ ] **SNMP trap receiver ingests real linkDown/linkUp traps and fires outage alerts**  —  🟢 P2 · ⬜ untested
  - **Run:** Configure a real device to send SNMPv1/v2c traps to the VigaBSS host (port 162 redirected to 1620), pull a link, and confirm the trap is stored in snmp_traps, mapped to the right device by source IP, and drives the event-bus -> alert/auto-outage hook.
  - **Why:** Trap reception depends on an environment-level iptables redirect (162 -> 1620) that no code or CI can set up; without it real traps silently never arrive. Source-IP -> device mapping and the trap-OID decode are only meaningful against a real agent's frames, and a broken map means proactive outage detection quietly does nothing.
  - **Needs:** Real device configured with VigaBSS as trap target + the 162->1620 UDP redirect (or root-bound 162).
- [ ] **NAS health check reports up/down correctly against a real MikroTik**  —  🟢 P2 · ⬜ untested
  - **Run:** Run nasHealthService.runHealthChecks against a real, healthy MikroTik and confirm it reports 'up' (and correctly flips to 'down' when the box is unreachable) without generating false transitions.
  - **Why:** probeNas sends a RADIUS Status-Server (code 12) to the CoA/DAE port 3799 and treats only an Access-Accept/Reject as 'alive'. MikroTik's radius-incoming listener does not answer Status-Server, so a perfectly healthy NAS can permanently read 'down' — spamming false NAS-down alerts and auto-outages — or the probe port/semantics may need to target 1812 instead. Only a real device reveals which.
  - **Needs:** Real MikroTik/CHR reachable on its RADIUS ports.

## Communications & subscriber portal

- [ ] **WhatsApp inbound webhook auth behind the real reverse proxy**  —  🔴 P0 · ⚪ unit-only
  - **Run:** Point a real Meta Cloud API (or Twilio WhatsApp) sandbox number at the production https://<domain>/api/v1/whatsapp/webhook URL through the actual nginx/Docker stack. Complete Meta's GET verification handshake (hub.mode/hub.verify_token/hub.challenge must echo back), then send a real inbound message and confirm the POST signature verifies and the message is processed (row in whatsapp_inbound_messages).
  - **Why:** The signature is the ONLY authentication on this public endpoint. Meta HMAC is over req.rawBody; Twilio HMAC is over a URL the server RECONSTRUCTS from X-Forwarded-Proto/X-Forwarded-Host (or WHATSAPP_WEBHOOK_PUBLIC_URL). Behind nginx the reconstructed URL (scheme, host, dropped/added port, /api path prefix) can differ byte-for-byte from what the provider signed, so EVERY inbound message silently 401s and the bot looks completely dead with nothing in logs but 'invalid signature'. And if WHATSAPP_VERIFY_TOKEN is wrong you can't even complete Meta subscription. Jest mocks the signature entirely, so this only surfaces against the real provider + real proxy.
  - **Needs:** Real Meta WhatsApp Cloud API app (APP_SECRET + VERIFY_TOKEN + phone number) or Twilio WhatsApp sandbox; publicly reachable webhook URL through prod nginx; correct WHATSAPP_WEBHOOK_PUBLIC_URL / X-Forwarded-* trust proxy config
- [ ] **WhatsApp binding round-trip with real outbound replies (email OTP + portal code)**  —  🔴 P0 · ⚪ unit-only
  - **Run:** From a real phone, message the business number: verify the welcome reply arrives, reply with an account email, confirm the 6-digit OTP email is received, reply the code, and confirm the 'You're connected' reply. Separately mint a portal linking code in the portal (Profile -> Connect WhatsApp), text it, and confirm binding. Confirm each bot reply is actually DELIVERED back to WhatsApp.
  - **Why:** config.validateEnv only WARNS (never fails) when WHATSAPP_APP_SECRET is set but WHATSAPP_PHONE_NUMBER_ID/ACCESS_TOKEN are missing: inbound is processed but every outbound reply throws and is swallowed (sendReply never throws), so the number binds server-side while the customer sees total silence and gives up. A wrong/expired Meta access token or an unapproved Twilio sender fails the same silent way. Replies must also land inside the provider's 24h customer-service window as free-form text. None of this is exercised by jest (outbound is mocked).
  - **Needs:** Real Meta/Twilio inbound AND outbound credentials (phone_number_id + access_token, or Twilio account+WhatsApp-from); real SMTP so the OTP email actually arrives; a real client row with a matching email
- [ ] **WhatsApp Wi-Fi password reset applied to a REAL TR-069-managed CPE**  —  🔴 P0 · ⚪ unit-only
  - **Run:** As a bound client with an active contract whose contract has a real CPE pointed at the VigaBSS ACS, run menu option 5, CONFIRM, and verify: (a) the new PSK email is delivered, (b) the portal_service_request goes to 'completed', (c) the physical router's Wi-Fi PSK actually changes on its next TR-069 Inform and devices must reconnect with the new key.
  - **Why:** The bot tells the customer 'Done - your new Wi-Fi password has been emailed, reconnect your devices' as soon as a CPE task is QUEUED (a cpe_devices row exists), but the PSK only truly changes when the real CPE informs the ACS and applies SetParameterValues. If the CPE isn't actually reachable/managed, the customer gets an emailed password that never takes effect (or the reverse) and is locked out of their own Wi-Fi with a support ticket the bot never opened. This write-action's safety (email-first, only-apply-if-delivered) is fully mocked in jest; the actual device round-trip is unverified.
  - **Needs:** A real TR-069/CWMP CPE (e.g. managed ONU/router) provisioned to the VigaBSS ACS URL; a contract linked to that cpe_devices row; real SMTP for PSK delivery
- [ ] **Per-org / per-function email delivery via real SMTP (invoices, OTP, Wi-Fi PSK, notifications)**  —  🔴 P0 · ⚪ unit-only
  - **Run:** Configure an organization's real SMTP identity for each function (general/support/billing/noc) in organization_email_settings, then trigger a send on each path: an invoice email (billing), a WhatsApp link-code OTP (general), a Wi-Fi password email (general), and a notification. Confirm each arrives from the correct From identity, and check email_logs status='sent'. Include an org whose 'support'/'noc' function is unconfigured to confirm the function->general->global fallback.
  - **Why:** emailTransport decrypts smtp_password_encrypted with ENCRYPTION_KEY and builds a nodemailer transport per org+function. Real SMTP exposes everything jest can't: wrong app-password, port 587 STARTTLS vs 465 secure mismatch, and — critically — SPF/DKIM/DMARC alignment between from_email's domain and the authenticated relay, which silently sends fiscally-important invoice mail to spam or gets it rejected. If ENCRYPTION_KEY differs from when the secret was saved, decrypt throws and every send for that org fails. Every failure just logs email_logs status='failed' and the customer never sees the invoice/OTP/password. jest mocks nodemailer, so deliverability is completely unverified.
  - **Needs:** A real org SMTP account/relay (Gmail app-password, SendGrid, or ISP relay) with valid SPF/DKIM on the from-domain; ENCRYPTION_KEY matching the stored encrypted secret; a real inbox to receive at
- [ ] **Portal invoice pay -> real Stripe hosted checkout -> webhook reconciliation**  —  🔴 P0 · ⚪ unit-only
  - **Run:** As a subscriber, POST /portal/invoices/:id/pay on a payable invoice, follow the returned Stripe-hosted checkout URL, complete a (test-mode) payment, and confirm Stripe's checkout.session.completed webhook reaches /api/v1/payment-webhooks/stripe(/:gatewayId), verifies its signature, and flips the invoice to 'paid' with the client balance credited (matched by gateway_reference_id = the cs_ session id).
  - **Why:** createCheckoutSession hits the real Stripe API with the org's decrypted secret key; reconciliation depends entirely on the webhook arriving at a publicly reachable URL with the correct signing secret (env STRIPE_WEBHOOK_SECRET vs the per-gateway payment_gateways.webhook_secret_encrypted — a known-fragile split). If the webhook URL/secret is misconfigured, Stripe captures the money but the invoice stays unpaid forever: the customer is charged while dunning/late-fees keep running, with no error anywhere. The round-trip is impossible to exercise without real Stripe keys and a reachable endpoint; jest mocks both the API and the webhook.
  - **Needs:** Real Stripe account (test-mode keys acceptable) configured as the org payment_gateway; publicly reachable webhook URL; correct webhook signing secret wired to the receiver that org actually uses
- [ ] **Outbound SMS / WhatsApp to Mexico via real Twilio**  —  🟠 P1 · ⚪ unit-only
  - **Run:** Send a real outbound SMS and a real outbound WhatsApp message to an MX (+52) handset via smsTransport.sendSms/queueSms (e.g. a payment reminder and an outage notification), and confirm delivery plus sms_logs status='sent' with a provider_message_id.
  - **Why:** Real Twilio to Mexico surfaces failures jest can't: Twilio geo-permissions for Mexico must be explicitly enabled or every send fails with error 21408; the SMS From must be a purchased/verified number and the WhatsApp From an approved WhatsApp sender; and any proactive WhatsApp sent OUTSIDE the 24h service window needs a pre-approved message template or fails with 63016. All of these just write sms_logs status='failed' — reminders and outage alerts silently never reach customers. Only Twilio REST call shapes are unit-tested.
  - **Needs:** Real Twilio account with MX geo-permissions enabled, a purchased SMS sender, an approved WhatsApp sender + approved templates for out-of-window sends; a real MX test handset
- [ ] **Portal password-reset email link works in production (APP_URL + global transport)**  —  🟠 P1 · ⚪ unit-only
  - **Run:** From the portal 'Forgot password' page, request a reset for a real portal-enabled client, receive the email, click the link, and complete the reset + login with the new password.
  - **Why:** Two production-only failure points: (1) the reset link is built from config.appUrl (APP_URL, default http://localhost:3000) — if APP_URL is unset/wrong in prod the emailed link points at the wrong host and no one can complete a reset; (2) unlike the WhatsApp OTP path, this sendEmail call passes NO organizationId, so it uses the GLOBAL env SMTP transport, not the org identity — on a multi-tenant install where orgs have SMTP configured but the global relay is not, reset emails silently never send. jest mocks the mailer and never renders/clicks the real link.
  - **Needs:** Correct production APP_URL; a working GLOBAL SMTP relay (SMTP_HOST/SMTP_FROM); a portal-enabled client with a reachable inbox
- [ ] **Service-request approval that mutates the live network (PPPoE, plan change)**  —  🟠 P1 · 🟡 sandbox-only
  - **Run:** Have a subscriber submit a pppoe_password_change and a plan_upgrade via the portal; as staff, approve each. Confirm the RADIUS password change actually re-authenticates the session on the real NAS, and the plan_upgrade's new speed profile actually takes effect on the subscriber's live connection.
  - **Why:** approveRequest writes radius.password and contracts.plan_id in the DB and marks the request 'completed', but a DB write alone does not change a live session — the NAS needs a CoA/disconnect to pick up a new password or speed tier. If nothing re-provisions the NAS, the customer's ACTUAL service is unchanged while the portal and staff UI both say 'completed', producing a silent 'I changed my password/plan but nothing happened' class of ticket. This is only observable against a real NAS/RADIUS.
  - **Needs:** A real NAS/RADIUS (RouterOS lab or production MikroTik) with an active PPPoE session; CoA/disconnect path wired; a real contract + plan
- [ ] **WhatsApp report-a-problem / human handoff / visit reaches staff, not just the DB**  —  🟠 P1 · ⚪ unit-only
  - **Run:** As a bound client, run option 4 (report a problem), option 7 (talk to a human), and option 6 (schedule a visit). Confirm a ticket / portal_service_request is created AND that a human on the staff side is actually alerted (bell/notification/queue) and the pending visit_schedule surfaces in the admin service-requests view for approval.
  - **Why:** createProblemTicket/createHumanHandoffTicket/scheduleVisit reliably INSERT rows, and the bot immediately tells the customer 'our team will follow up' / 'a team member will reach out'. But if no notification hook fires and the pending request isn't surfaced in a worklist, the ticket sits unseen: the customer was promised human follow-up that never comes. The insert is unit-tested; whether staff are actually notified/surfaced end-to-end is not.
  - **Needs:** Running app with notification hooks/bell wired; a staff account watching the tickets + admin service-requests queues (partially checkable in sandbox, but notification delivery needs real config)
- [ ] **MX phone normalization vs real provider sender-ID formats**  —  🟢 P2 · ⚪ unit-only
  - **Run:** Bind a Mexican number, then send follow-up messages and confirm the SAME physical number resolves to the existing binding whether the provider delivers it as 52XXXXXXXXXX or 521XXXXXXXXXX (and with/without the whatsapp: prefix). Confirm a bound customer never falls back to the 'welcome/verify' flow.
  - **Why:** normalizeE164 strips the legacy MX-mobile '1' (521 -> 52) so both forms WhatsApp may deliver converge to one key. If a real provider delivers a format the normalizer doesn't fold (or the trust-proxy/prefix handling differs from assumptions), a bound customer's number won't match whatsapp_links.active_phone and the bot greets them as a stranger on every message — or worse, creates a second binding. Real Meta and Twilio format MX sender IDs differently; jest only tests the pure function against assumed strings.
  - **Needs:** A real MX WhatsApp handset messaging via the actual Meta/Twilio provider (to observe the real delivered From format)

## Platform, ops, security, deploy

- [ ] **One-command install.sh + TLS on a clean Ubuntu VM**  —  🔴 P0 · ⬜ untested
  - **Run:** On a fresh Ubuntu/Debian VM with a real public domain + DNS pointing at it, run `curl -fsSL .../install.sh | DOMAIN=... EMAIL=... bash` end to end: Docker CE auto-install, secret auto-generation into .env.prod (chmod 600), Let's Encrypt HTTP-01 (and CF_API_TOKEN DNS-01) cert issuance, `docker compose up --build`, the 5-min DB wait, `migrate.js`, `seed.js`, admin account creation, /health = 200, and the `fireisp` CLI wrapper. Also exercise the USE_HOST_NGINX=1 branch (port 80 already bound) and the SKIP_TLS self-signed fallback.
  - **Why:** The entire product ships as this script; it is executed by mocked jest never. Silent breaks: Let's Encrypt failure silently falls back to a self-signed cert (browsers reject, install looks 'done'); the port-80 auto-detect mis-routes to host-nginx; seed.js failing after migrate leaves a schema with no admin and no way in; the printed ADMIN_PASSWORD is the only copy. A broken installer = zero customers can deploy.
  - **Needs:** Fresh cloud VM (no Docker), a real registrable domain with DNS control, port 80/443 reachable from the internet
- [ ] **All 420 migrations apply cleanly from zero on real MySQL 8.4 (+ replica propagation, idempotent re-run)**  —  🔴 P0 · 🟡 sandbox-only
  - **Run:** Point migrate.js at an empty MySQL 8.4.9 instance (the prod image) and apply all 420 files in order; then run it a second time to confirm it is a no-op (idempotent, schema_migrations honored). Confirm DELIMITER/stored-proc files split correctly, GENERATED columns and INFORMATION_SCHEMA-guarded ALTERs succeed, and every DDL propagates to the GTID db-replica without breaking replication. Cross-check `pnpm run schema:parity` and `sql:check` against the resulting live DB.
  - **Why:** jest mocks the DB (`jest.mock('../config/database')`) so migration SQL is never executed by any engine in CI. A migration that only fails on a truly empty DB (forward reference, charset/collation, a MySQL-8.4-specific or replication-unsafe statement) blocks every fresh install and can silently break the read replica while the primary looks fine. schema.sql drift means restores/new installs diverge from upgraded ones.
  - **Needs:** Real MySQL 8.4 (ideally the docker-compose.prod.yml primary+replica with GTID), empty DB; a MariaDB run too if MariaDB is a supported target
- [ ] **Multi-tenant cross-org isolation (adversarial)**  —  🔴 P0 · ⚪ unit-only
  - **Run:** Seed 2+ organizations with real rows. Authenticated as org A, attempt to read/modify org B's resources by ID across the leak-prone surfaces — clients, invoices, payments, devices, CFDI docs, reports/exports, snmp-metrics, reseller-scoped rows — and via switch-organization and an API token scoped to org A. Every attempt must 404/403, never return org B data. Grep-driven: focus on raw `db.query` handlers and `SELECT *` paths that could omit the org filter.
  - **Why:** orgScope sets req.orgId from the JWT and models scope by it, but the historical #1 bug class is a raw query or SELECT * that forgets the org predicate — and jest mocks the DB, so a missing WHERE organization_id is invisible in unit tests. A single leaking endpoint is a cross-tenant data breach on a shared install. Prior sweeps already found real cross-tenant leaks in snmp-metrics.
  - **Needs:** Real MySQL seeded with two orgs + data; two user tokens; one org-scoped API token
- [ ] **Every role's granted pages load with no 403 on visible rows**  —  🔴 P0 · 🟡 sandbox-only
  - **Run:** On a freshly seeded prod DB, log in as each persona — admin, manager, technician, support, billing, readonly, reseller, subscriber (portal) — and walk every nav-registry page that lists that role, confirming each page's API calls return data (not 403) and readonly never crashes. Spot-check the action buttons the role is supposed to have. Verify against the actual role_permissions seed, not navPersonas.test assumptions.
  - **Why:** An unseeded permission slug = 403 for everyone but legacy admin, silently; a nav row marked visible-for-role that the seed didn't actually grant renders a broken page. This is the platform's signature end-to-end break. navPersonas.test.ts mocks getPermissions so it validates the registry against itself, not against what the real seed grants. Only a real login per role on a real seed proves the grant chain.
  - **Needs:** Real seeded DB (default roles/permissions from seed.js) + one account per role; the built frontend served
- [ ] **Auth/session behind the real Nginx proxy: trust-proxy IP, rate limits, lockout**  —  🔴 P0 · 🟡 sandbox-only
  - **Run:** With the full Docker stack (Nginx → app, TRUST_PROXY=1), verify: req.ip resolves to the real client IP not the proxy (hit an endpoint that echoes it / check per-IP behavior from two client IPs); the 5-failed-login lockout triggers per-account and auto-unlocks; the strict authLimiter 429s brute-force on /login while session endpoints (/auth/me,/refresh,/switch-organization) do NOT 429 under a busy dashboard's reload storm; JWT signed with the generated 64-char HS256 secret verifies and refresh rotates.
  - **Why:** If trust proxy is wrong, every request appears to come from the proxy IP: all per-IP rate-limit and lockout buckets collapse into one shared bucket — which historically starved /auth/refresh and logged all active users out (PR #377), and also means one attacker's failures lock out everyone or nobody. This only manifests with a real proxy in front; unit tests hit the app directly with no X-Forwarded-For chain.
  - **Needs:** Full docker-compose.prod stack (real Nginx in front of the app), two distinct client source IPs
- [ ] **ENCRYPTION_KEY production hard-stop + secrets-at-rest round trip**  —  🔴 P0 · 🟡 sandbox-only
  - **Run:** Boot with NODE_ENV=production and ENCRYPTION_KEY unset → the process must FATAL at validateEnv (not warn-and-continue). With a valid 64-hex key: save a payment-gateway secret, a PAC/CSD password, and a per-gateway webhook secret, then inspect the DB columns to confirm they hold ciphertext (not plaintext) and decrypt correctly on read. Finally boot with a DIFFERENT key against that data and confirm decrypt fails loudly, not silently returns garbage.
  - **Why:** A production install that starts with no key stores every gateway credential, PAC password, and CSD key in plaintext — a breach waiting to happen; the hard-stop is the only thing preventing it and it's environment-gated (config.env === 'production'), so it can't be exercised by the test-env jest run. A lost/rotated key silently orphaning stored secrets is equally catastrophic for CFDI stamping and payments.
  - **Needs:** Real MySQL + a genuine production-env boot (NODE_ENV=production) with and without ENCRYPTION_KEY
- [ ] **CFDI stamp + cancel against a REAL PAC with a REAL CSD**  —  🔴 P0 · 🟡 sandbox-only
  - **Run:** With a real PAC production contract (SW and/or Finkok) and a real (non-test) CSD uploaded + encrypted, stamp a real invoice for a real receptor RFC, get a live SAT UUID, download the signed XML + representación-impresa PDF, then CANCEL it and confirm a real SAT acuse. Verify the test-CSD-vs-prod guard blocks a test cert in prod, and that quota/timbre accounting is correct.
  - **Why:** All CFDI stamping to date is SANDBOX only with TEST CSDs — production SAT validates the receptor against the real LCO, enforces the real CSD, and consumes paid timbres. Known open risk: Finkok cancel has an unresolved xmldsig format bug ('Invalid Signature'); launch depends on the SW failover actually covering cancellation in production, or cancel silently fails and invoices can't be voided — a legal/fiscal blocker for a Mexican ISP.
  - **Needs:** A real PAC contract (SW Sapien and/or Finkok WS creds — still pending per notes), a real CSD (.cer/.key) for a real RFC, real receptor RFCs, a real ENCRYPTION_KEY
- [ ] **Backup → real RESTORE-and-boot (DR drill phases 2-3) + remote S3 upload**  —  🟠 P1 · 🟡 sandbox-only
  - **Run:** Run `npm run backup` on a populated real DB, then actually perform the documented restore (docs/backup-restore.md): gunzip the dump into a fresh database and boot the app against it — verify clients/invoices/CFDI/schema_migrations counts and that the app runs. Separately configure a real S3-compatible target (backup_settings row or BACKUP_S3_* env) and confirm the gzip lands in the bucket with correct SigV4 auth. Then run the automated /dr-drill and confirm pass.
  - **Why:** The automated DR drill only does phase 1 (dump + gzip-structure check) and phase 4 (integrity queries) — the RESTORE itself (phases 2-3) is manual and has never been drilled to a booted app. A dump that can't restore (FK/routine/trigger/event privilege gaps, --no-tablespaces, charset) is worse than no backup. The 20-byte empty-gzip bug class lived here for months. Remote upload + SigV4 only fails against a real bucket.
  - **Needs:** Real MySQL with production-like data; a real S3-compatible endpoint (AWS S3 / MinIO / B2 / R2) with credentials; a scratch DB to restore into
- [ ] **Payment + WhatsApp webhooks fail-closed against REAL provider signatures**  —  🟠 P1 · ⚪ unit-only
  - **Run:** Point a real Stripe test-mode webhook at /api/v1/payment-webhooks/stripe (and the /:gatewayId tenant variant) with the real signing secret: a genuine signed event → 200 and reconciles; a tampered body → 401; secret unset → 503 (never silently trusted); non-{id,type} probe → 400. Repeat for Meta WhatsApp Cloud API (X-Hub-Signature-256 over the raw body + the GET verify handshake) and Twilio (X-Twilio-Signature). Confirm the express.json `verify` raw-body capture in app.js fires for the exact mounted URL prefixes.
  - **Why:** Signature verification is HMAC over the exact received bytes; the raw-body capture is keyed on a hardcoded URL-prefix list in app.js — a prefix/mount mismatch silently disables verification (or breaks a valid signature) while unit tests, which fabricate their own signatures over their own bytes, still pass. Getting the real provider's signature format + header name + raw-body plumbing right is only provable with the real provider POSTing.
  - **Needs:** Real Stripe test-mode account + webhook signing secret; a real Meta WhatsApp app (app secret + verify token) and/or Twilio auth token; a public HTTPS endpoint the providers can reach
- [ ] **Stripe hosted checkout money-movement end to end**  —  🟠 P1 · ⬜ untested
  - **Run:** Configure a real Stripe gateway (secret key stored encrypted via ENCRYPTION_KEY), create a checkout session for a payable invoice, pay with a Stripe test card, receive the webhook, and confirm the invoice flips to paid, payment_transactions reconciles the real gateway reference, and any CFDI/credit follow-on fires. Verify MXN→cents conversion, idempotency-key replay safety, and the same-origin return-url guard.
  - **Why:** chargeStripe() calls api.stripe.com live via raw https and decrypts the real secret key at call time — a path that has never run against real Stripe. Silent breaks: wrong amount (cents/currency), decrypt failing with the real ENCRYPTION_KEY, idempotency double-charge, or the transaction row stuck 'pending' if the webhook leg doesn't line up. If you advertise online payment at launch and this is wrong, you either don't get paid or double-charge.
  - **Needs:** Real Stripe test-mode keys, a test card, the webhook wired (overlaps the webhook test), a real ENCRYPTION_KEY set
- [ ] **FireRelay remote NAS tunnel over real WSS with a live agent**  —  🟠 P1 · ⬜ untested
  - **Run:** Set FIRERELAY_TUNNEL_SECRET, run `pnpm run firerelay:agent` on a remote POP box behind NAT, have it connect outbound over WSS to /ws/firerelay through the real Nginx (which proxies the Upgrade), authenticate (node_id + shared secret), then push a real RouterOS command (provision a PPPoE user / issue a CoA) to a NAS reachable ONLY through the tunnel and confirm the response returns. Kill the link and confirm auto-reconnect; confirm a bad secret is rejected.
  - **Why:** The tunnel is disabled unless the secret is set and is the only path to NASes behind CGNAT/NAT at remote POPs — a core multi-site ISP capability that no jest test drives (it needs a live WebSocket peer). Nginx must proxy the WS Upgrade correctly, the auth handshake and command/response correlation must survive real network latency and drops, and reconnect must actually re-register the node. If broken, remote-POP provisioning and CoA silently never reach the router.
  - **Needs:** A real remote RouterOS/NAS behind NAT, the FireRelay agent running there, FIRERELAY_TUNNEL_SECRET set, WSS reachable through the production Nginx
- [ ] **SMTP transactional-mail deliverability for mail-gated flows**  —  🟠 P1 · ⬜ untested
  - **Run:** Configure a real SMTP relay (install.sh ships SMTP_HOST=localhost with blank creds) and confirm each mail-gated flow actually sends AND lands (not spam-foldered): password reset, email verification, invoice delivery, payment reminders, and backup/DR-failure alerts. Check SPF/DKIM/DMARC alignment for the SMTP_FROM=noreply@<domain> and that the mail-bomb rate limits (passwordResetLimiter, verifyEmailResendLimiter) don't block legitimate resends.
  - **Why:** The installer leaves SMTP unconfigured, so a fresh install silently no-ops all outbound mail — most dangerously password reset and email verification, meaning a locked-out admin (see the 5-attempt lockout) can never recover, and customers never get invoices or reminders. emailTransport is mocked in every unit test, so deliverability + auth + alignment can only be proven against a real relay.
  - **Needs:** A real SMTP relay/credentials (or provider like SES/Postmark), a domain with SPF/DKIM/DMARC set, a real inbox to receive at
