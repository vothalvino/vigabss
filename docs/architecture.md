# Architecture documentation moved

The canonical, agent-neutral architecture and engineering contract is now
[`ARCHITECTURE.md`](../ARCHITECTURE.md) at the repository root.

This forwarding page is retained so existing documentation links do not lead to
a stale second source of truth. Update the root document when an architectural
boundary, data flow, public contract, runtime, deployment topology, or required
validation command changes.
