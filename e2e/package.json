{
  "name": "vigabss-e2e",
  "version": "1.0.0",
  "description": "VigaBSS 5.0 — Playwright end-to-end smoke tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
