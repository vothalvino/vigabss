// =============================================================================
// VigaBSS 5.0 — Import Validation Schemas
// =============================================================================

const importCsv = {
  csv: { type: 'string', required: true, min: 1 },
};

module.exports = { importCsv };
