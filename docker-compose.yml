services:
  db:
    image: mysql:8.4.9
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-vigabss_root}
      MYSQL_DATABASE: ${DB_NAME:-vigabss}
    ports:
      - '3306:3306'
    volumes:
      - db_data:/var/lib/mysql
    command: >
      --event-scheduler=ON
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --host-cache-size=0
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --maxmemory 128mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 64M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  app:
    build: .
    restart: unless-stopped
    ports:
      - '3000:3000'
      # Embedded RADIUS server (off by default in dev — set RADIUS_SERVER_ENABLED=true)
      - '${RADIUS_AUTH_PORT:-1812}:1812/udp'
      - '${RADIUS_ACCT_PORT:-1813}:1813/udp'
      # Unsolicited SNMP traps. Devices must send to this host/UDP port.
      # Keep >=1024 for the non-root development container (default 1620).
      - '${SNMP_TRAP_PUBLISH_IP:-127.0.0.1}:${SNMP_TRAP_PORT:-1620}:${SNMP_TRAP_PORT:-1620}/udp'
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DB_HOST: db
      DB_PORT: 3306
      REDIS_URL: redis://redis:6379
      VECTOR_RETRIEVAL_ENABLED: ${VECTOR_RETRIEVAL_ENABLED:-false}
      CHROMA_URL: ${CHROMA_URL:-http://chromadb:8000}
      RADIUS_SERVER_ENABLED: ${RADIUS_SERVER_ENABLED:-false}
      RADIUS_AUTH_PORT: ${RADIUS_AUTH_PORT:-1812}
      RADIUS_ACCT_PORT: ${RADIUS_ACCT_PORT:-1813}
      SNMP_TRAP_PORT: ${SNMP_TRAP_PORT:-1620}
      # Bind inside the container; SNMP_TRAP_PUBLISH_IP above is the host ACL.
      SNMP_TRAP_BIND_IP: 0.0.0.0
    volumes:
      - ./src:/app/src
      - ./storage:/app/storage
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

  chromadb:
    image: chromadb/chroma:1.0.4
    profiles: ['rag']
    restart: unless-stopped
    ports:
      - '8000:8000'
    volumes:
      - chroma_data:/chroma/chroma
    environment:
      IS_PERSISTENT: 'TRUE'
      ANONYMIZED_TELEMETRY: 'FALSE'
    healthcheck:
      test: ['CMD-SHELL', 'curl -sf http://localhost:8000/api/v1/heartbeat || exit 1']
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M
    logging:
      driver: json-file
      options:
        max-size: '10m'
        max-file: '3'

volumes:
  db_data:
  redis_data:
  chroma_data:
