# pnpm configuration for VigaBSS 0.1.0-alpha.1
auto-install-peers=true
# Lifecycle scripts are restricted to the packages in pnpm.onlyBuiltDependencies
# (see package.json) — esbuild, msgpackr-extract, and unrs-resolver need native
# postinstall scripts to download the correct platform binary for Vite/Vitest.
ignore-scripts=false
